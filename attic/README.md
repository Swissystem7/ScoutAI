# Attic — not the product

Nothing in this folder is loaded by the live lab (`index.html`, `demo.js`, `trap/`).
Keep it only as history.

## What is here

- `proof-demo.js` — the circular «face energy» script the trap lesson quotes. The trap page copies the **hand-typed numbers**, and does not run this file or any face model.
- Other `proof-*.js` scripts — they imported a factory `lib/` engine that was removed. Their printed rankings are **not reproducible**.
- `research-data/` — unused snapshots (FBref Big-5, off-ball, body, xT, old PoC JSON). Not wired into the demo. Do not sell analysis derived from them.
- `docs/` — stale product notes (hackathon pitch, 49 ₪ reports, video-pipeline plan, old PoC write-ups). They describe a product that was pivoted away from.

## What the live demo reads

Only `data/wc2018_event_aggregates.json` (and, if the user asks, `data/user-dataset.example.json` or a local file they attest they licensed).
